spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      javax:
        persistence:
          validation:
            mode: none
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: myBlog
  rabbitmq:
    host: localhost
    port: 5672
    username: mstgnz
    password: mstgnz

